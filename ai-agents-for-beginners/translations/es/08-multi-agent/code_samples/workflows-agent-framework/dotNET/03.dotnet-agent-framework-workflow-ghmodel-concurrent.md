# ⚡ Flujos de trabajo concurrentes de agentes con Azure OpenAI (API de respuestas) (.NET)

## 📋 Tutorial de procesamiento paralelo de alto rendimiento

Este cuaderno demuestra **patrones de flujo de trabajo concurrentes** usando el Microsoft Agent Framework para .NET y Azure OpenAI (API de respuestas). Aprenderás a construir flujos de trabajo de procesamiento paralelo de alto rendimiento que maximizan el rendimiento ejecutando múltiples agentes de IA simultáneamente mientras mantienen la coordinación y la consistencia de datos.

## 🎯 Objetivos de aprendizaje

### 🚀 **Fundamentos del procesamiento concurrente**
- **Ejecución paralela de agentes**: Ejecuta múltiples agentes de IA simultáneamente para un rendimiento máximo
- **Patrones Async/Await**: Aprovecha el modelo de programación async de .NET para una concurrencia eficiente
- **Azure OpenAI (API de respuestas)**: Coordina múltiples llamadas concurrentes a la API de respuestas de Azure OpenAI
- **Gestión de recursos**: Gestiona eficientemente los recursos del modelo de IA a través de operaciones concurrentes

### 🏗️ **Arquitectura avanzada de concurrencia**
- **Paralelismo basado en tareas**: Usa la Biblioteca Paralela de Tareas de .NET para una ejecución concurrente óptima
- **Patrones de sincronización**: Coordina agentes concurrentes evitando condiciones de carrera
- **Balanceo de carga**: Distribuye el trabajo eficientemente entre la capacidad de procesamiento concurrente disponible
- **Tolerancia a fallos**: Maneja fallos individuales de agentes sin detener todo el flujo de trabajo

### 🏢 **Aplicaciones empresariales concurrentes**
- **Procesamiento de documentos de alto volumen**: Procesa múltiples documentos simultáneamente
- **Análisis en tiempo real de contenido**: Análisis concurrente de flujos de datos entrantes
- **Optimización de procesamiento por lotes**: Maximiza el rendimiento para operaciones de procesamiento de datos a gran escala
- **Análisis multimodal**: Procesamiento paralelo de diferentes tipos de contenido y formatos

## ⚙️ Prerrequisitos y configuración

### 📦 **Paquetes NuGet requeridos**

Paquetes esenciales para flujos de trabajo concurrentes de alto rendimiento:

```xml
<!-- Core AI Framework with Async Support -->
<PackageReference Include="Microsoft.Extensions.AI" Version="10.*" />

<!-- Azure OpenAI (Responses API) -->
<PackageReference Include="Azure.AI.OpenAI" Version="2.*" />

<!-- Azure Identity and Async LINQ for Advanced Operations -->
<PackageReference Include="Azure.Identity" Version="1.15.0" />
<PackageReference Include="System.Linq.Async" Version="6.0.3" />

<!-- Local Agent Framework References -->
<!-- Microsoft.Agents.AI.dll - Core agent abstractions with async support -->
<!-- Microsoft.Agents.AI.OpenAI.dll - Azure OpenAI (Responses API) integration with concurrency -->
```

### 🔑 **Configuración de Azure OpenAI**

**Configuración del entorno (archivo .env):**
```env
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-5-mini
```

**Consideraciones para el procesamiento concurrente:**
```csharp
// Configure for concurrent operations
var clientOptions = new AzureOpenAIClientOptions()
{
    // Configure network timeout for concurrent requests
    NetworkTimeout = TimeSpan.FromMinutes(5)
};
```

### 🏗️ **Arquitectura del flujo de trabajo concurrente**

```mermaid
graph TD
    A[Entrada del Flujo de Trabajo] --> B[Distribución de Tareas]
    B --> C[Grupo Concurrente de Agentes]
    C --> D[Tarea del Agente 1]
    C --> E[Tarea del Agente 2]
    C --> F[Tarea del Agente 3]
    C --> G[Tarea del Agente N]
    
    D --> H[Agregación de Resultados]
    E --> H
    F --> H
    G --> H
    
    H --> I[Salida Sincronizada]
    
    J[Azure OpenAI (API de Respuestas)] --> D
    J --> E
    J --> F
    J --> G
    
    K[Programador de Tareas .NET] --> C
```

**Componentes clave:**
- **Biblioteca de tareas paralelas**: Soporte integrado de .NET para operaciones concurrentes
- **Grupo de agentes**: Múltiples instancias de agentes para procesamiento paralelo
- **Agregación de resultados**: Coordinación y combinación de resultados concurrentes de agentes
- **Puntos de sincronización**: Aseguran la consistencia de datos en operaciones concurrentes

## 🎨 **Patrones de diseño de flujo de trabajo concurrente**

### 🔍 **Investigación y análisis paralelo**
```
Research Topic → Concurrent Research Agents → Result Synthesis → Final Report
```

### 📊 **Procesamiento de datos de múltiples fuentes**
```
Data Sources → Parallel Processing Agents → Data Integration → Unified Output
```

### 🎭 **Canalización de generación de contenido**
```
Content Requirements → Concurrent Content Generators → Quality Review → Final Content
```

### 🔄 **Procesamiento Fan-Out/Fan-In**
```
Single Input → Multiple Concurrent Processors → Result Aggregation → Single Output
```

## 🏢 **Beneficios de rendimiento empresariales**

### ⚡ **Rendimiento y escalabilidad**
- **Escalado lineal del rendimiento**: Agrega más agentes concurrentes para aumentar el rendimiento
- **Utilización de recursos**: Máxima eficiencia de la capacidad del modelo de IA disponible
- **Reducción del tiempo de procesamiento**: Reducción significativa del tiempo mediante ejecución paralela
- **Escalado elástico**: Ajuste dinámico del conteo de agentes concurrentes según la carga de trabajo

### 🛡️ **Confiabilidad y resiliencia**
- **Aislamiento de fallos**: Fallos individuales de agentes no afectan otras operaciones concurrentes
- **Degradación gradual**: El sistema continúa operando con capacidad reducida de agentes
- **Recuperación de errores**: Mecanismos automáticos de reintento para operaciones concurrentes fallidas
- **Distribución de carga**: Distribución equitativa del trabajo entre agentes disponibles

### 📊 **Monitoreo de desempeño**
- **Métricas de ejecución concurrente**: Seguimiento del desempeño de todas las operaciones paralelas
- **Análisis de uso de recursos**: Monitoriza CPU, memoria y uso de red
- **Análisis de rendimiento**: Mide ganancias de eficiencia gracias al procesamiento concurrente
- **Detección de cuellos de botella**: Identifica y resuelve limitaciones de rendimiento

### 🔧 **Desarrollo y operaciones**
- **Modelo de programación Async**: Aprovecha los patrones async/await maduros de .NET
- **Coordinación de tareas**: Capacidades integradas de gestión y coordinación de tareas
- **Manejo de excepciones**: Manejo completo de errores para operaciones concurrentes
- **Soporte para depuración**: Herramientas de depuración de Visual Studio para flujos de trabajo concurrentes

¡Construyamos flujos de trabajo concurrentes de IA de alto rendimiento con .NET! 🚀

## 💻 Ejecución del código

La implementación completa está disponible en `03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs`. Este archivo demuestra un **flujo de trabajo concurrente Fan-Out/Fan-In** para planificación de viajes:

### 🏗️ **Arquitectura del flujo de trabajo**

```
User Request → ConcurrentStartExecutor → [Researcher Agent || Planner Agent] → ConcurrentAggregationExecutor → Final Output
```

**Componentes clave:**

1. **ConcurrentStartExecutor**: Transmite la solicitud del usuario a todos los agentes simultáneamente
2. **Agente Investigador**: Analiza destinos y atracciones concurrentemente
3. **Agente Planificador**: Crea planes de viaje detallados concurrentemente
4. **ConcurrentAggregationExecutor**: Recoge y combina los resultados de ambos agentes

### 🎯 **Patrón Fan-Out/Fan-In**

Este flujo de trabajo demuestra el clásico patrón **Fan-Out/Fan-In**:
- **Fan-Out**: Un mensaje de entrada se transmite a múltiples agentes simultáneamente
- **Procesamiento concurrente**: Múltiples agentes trabajan en paralelo en la misma tarea
- **Fan-In**: Los resultados de todos los agentes se recogen y agregan en una salida única

### 🚀 Ejecución del ejemplo

```bash
# Hacer el script ejecutable (Unix/Linux/macOS)
chmod +x 03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs

# Ejecutar el flujo de trabajo concurrente
./03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs
```

O en Windows:
```powershell
dotnet run 03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs
```

### 📝 Salida esperada

El flujo de trabajo hará:
1. **Transmitir solicitud**: Enviar "Planifica un viaje a Seattle en diciembre" a ambos agentes
2. **Procesamiento concurrente**: Ambos agentes trabajan simultáneamente:
   - Investigador identifica atracciones y detalles
   - Planificador crea itinerario y logística
3. **Agregación**: Combina ambas respuestas en una salida comprensiva
4. **Mostrar resultados**: Muestra el plan de viaje combinado con toda la información

### 🔧 Opciones de personalización

**Agregar más agentes concurrentes:**
```csharp
// Create additional specialized agents
AIAgent budgetAgent = azureClient.GetChatClient(deployment).AsAIAgent(
    name: "Budget-Agent", instructions: "Calculate travel costs...");

// Add to fan-out
var workflow = new WorkflowBuilder(startExecutor)
    .AddFanOutEdge(startExecutor, targets: [researcherAgent, plannerAgent, budgetAgent])
    .AddFanInBarrierEdge(sources: [researcherAgent, plannerAgent, budgetAgent], target: aggregationExecutor)
    .WithOutputFrom(aggregationExecutor)
    .Build();

// Update aggregation count
if (this._messages.Count == 3) { ... }
```

**Modificar instrucciones de agentes:**
```csharp
const string ResearcherAgentInstructions = "Your custom instructions for research...";
const string PlanAgentInstructions = "Your custom instructions for planning...";
```

**Cambiar la tarea:**
```csharp
StreamingRun run = await InProcessExecution.RunStreamingAsync(
    workflow, 
    "Plan a European vacation for 2 weeks in summer"
);
```

### 🎯 Aplicaciones en el mundo real

Este patrón concurrente es ideal para:
- **Creación de contenido**: Múltiples escritores creando secciones diferentes simultáneamente
- **Revisión de código**: Múltiples revisores analizando código desde diferentes perspectivas
- **Investigación de mercado**: Análisis paralelo de distintos segmentos de mercado
- **Procesamiento de documentos**: Extracción, análisis y validación concurrentes
- **Análisis multiperspectiva**: Obteniendo puntos de vista diversos sobre la misma entrada

### 🔍 Comprendiendo ejecutores personalizados

**ConcurrentStartExecutor:**
- Implementa `IMessageHandler<string>` para aceptar entrada de tipo string
- Transmite mensajes a todos los agentes conectados
- Envía `TurnToken` para disparar el procesamiento concurrente

**ConcurrentAggregationExecutor:**
- Implementa `IMessageHandler<ChatMessage>` para recibir respuestas de agentes
- Recopila mensajes de forma segura para subprocesos (thread-safe)
- Agrega cuando llegan todas las respuestas esperadas
- Produce la salida final usando `context.YieldOutputAsync()`

### ⚡ Beneficios de rendimiento

**Concurrente vs Secuencial:**
- Secuencial: Agente1 (30s) → Agente2 (30s) = **60 segundos total**
- Concurrente: Agente1 (30s) || Agente2 (30s) = **30 segundos total**

**Mejora de rendimiento**: Hasta N× más rápido para N agentes concurrentes (dependiendo de la carga de trabajo y recursos)

### 🛡️ Manejo de errores

El flujo de trabajo maneja fallos de agentes individuales con gracia:
- Si un agente falla, los demás continúan procesando
- El agregador puede implementar lógica de tiempo de espera (timeout)
- Se pueden devolver resultados parciales si es necesario

### 📊 Características avanzadas

**Conteo dinámico de agentes:**
Modifica la lógica de agregación para soportar conteos variables de agentes:

```csharp
private int _expectedAgentCount;
private readonly List<ChatMessage> _messages = [];

public override ValueTask HandleAsync(ChatMessage message, IWorkflowContext context, CancellationToken cancellationToken = default)
{
    this._messages.Add(message);
    if (this._messages.Count == _expectedAgentCount)
    {
        // Process aggregation
    }
    
    return ValueTask.CompletedTask;
}
```

¡Este patrón de flujo de trabajo concurrente es esencial para construir sistemas de agentes de IA escalables y de alto rendimiento!

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Descargo de responsabilidad**:
Este documento ha sido traducido utilizando el servicio de traducción automática [Co-op Translator](https://github.com/Azure/co-op-translator). Aunque nos esforzamos por la precisión, tenga en cuenta que las traducciones automatizadas pueden contener errores o inexactitudes. El documento original en su idioma nativo debe considerarse la fuente autorizada. Para información crítica, se recomienda una traducción profesional humana. No somos responsables de cualquier malentendido o interpretación errónea que surja del uso de esta traducción.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->