# ⏩ گردش‌های کاری متوالی عامل با Azure OpenAI (API پاسخ‌ها) (.NET)

## 📋 آموزش پیشرفته پردازش متوالی

این دفترچه یادداشت الگوهای **گردش کاری متوالی** را با استفاده از چارچوب عامل مایکروسافت برای .NET و Azure OpenAI (API پاسخ‌ها) نشان می‌دهد. شما یاد می‌گیرید چگونه خطوط لوله پردازش پیچیده و گام‌به‌گام بسازید که در آن عوامل به ترتیب خاصی اجرا می‌شوند، و هر مرحله بر نتایج مرحله قبلی بنا می‌شود.

## 🎯 اهداف یادگیری

### 🔄 **معماری پردازش متوالی**
- **طراحی گردش کار خطی**: ایجاد خطوط لوله پردازش گام به گام با وابستگی‌های واضح
- **مدیریت حالت**: حفظ زمینه و جریان داده در سراسر مراحل متوالی گردش کار
- **Azure OpenAI (API پاسخ‌ها)**: بهره‌گیری از مدل‌های Azure OpenAI در گردش‌های کاری چند مرحله‌ای .NET
- **الگوهای خط لوله سازمانی**: ساخت سیستم‌های پردازش متوالی آماده تولید

### 🏗️ **الگوهای پیشرفته متوالی**
- **پردازش مرحله-دروازه**: پیاده‌سازی نقاط بازرسی اعتبارسنجی بین مراحل گردش کار
- **حفظ زمینه**: حفظ حالت و دانش انباشته در سراسر همه مراحل
- **انتشار خطا**: مدیریت خطاها به صورت مناسب در زنجیره‌های پردازش متوالی
- **بهینه‌سازی عملکرد**: اجرای متوالی کارآمد با حداقل سربار

### 🏢 **برنامه‌های متوالی سازمانی**
- **خط لوله پردازش اسناد**: تحلیل چند مرحله‌ای اسناد، تبدیل و اعتبارسنجی
- **گردش‌های کاری تضمین کیفیت**: بازبینی، اعتبارسنجی و فرایندهای تصویب متوالی
- **خط لوله تولید محتوا**: تحقیق → نوشتن → ویرایش → بازبینی → انتشار
- **اتوماسیون فرآیند کسب‌وکار**: گردش‌های کاری کسب‌وکار چند مرحله‌ای با وابستگی‌های واضح مرحله‌ای

## ⚙️ پیش‌نیازها و راه‌اندازی

### 📦 **بسته‌های NuGet مورد نیاز**

بسته‌های ضروری برای گردش‌های کاری متوالی .NET:

```xml
<!-- Core AI Framework -->
<PackageReference Include="Microsoft.Extensions.AI" Version="10.*" />

<!-- Azure OpenAI (Responses API) -->
<PackageReference Include="Azure.AI.OpenAI" Version="2.*" />

<!-- Azure Identity and Async LINQ Support -->
<PackageReference Include="Azure.Identity" Version="1.15.0" />
<PackageReference Include="System.Linq.Async" Version="6.0.3" />

<!-- Local Agent Framework References -->
<!-- Microsoft.Agents.AI.dll - Core agent abstractions -->
<!-- Microsoft.Agents.AI.OpenAI.dll - Azure OpenAI (Responses API) integration -->
```

### 🔑 **پیکربندی Azure OpenAI**

**راه‌اندازی محیط (.env فایل):**
```env
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-4.1-mini
```

**مدیریت پیکربندی:**
```csharp
// Load environment variables securely
Env.Load("../../../.env");
var azureEndpoint = Environment.GetEnvironmentVariable("AZURE_OPENAI_ENDPOINT");
var deployment = Environment.GetEnvironmentVariable("AZURE_OPENAI_DEPLOYMENT");
```

### 🏗️ **معماری گردش کار متوالی**

```mermaid
graph TD
    A[Initial Input] --> B[Stage 1: عامل تحلیل]
    B --> C[نقطه بررسی ۱]
    C --> D[Stage 2: عامل پردازش]
    D --> E[نقطه بررسی ۲]
    E --> F[Stage 3: عامل اعتبارسنجی]
    F --> G[خروجی نهایی]
    
    H[زمینه وضعیت] --> B
    H --> D
    H --> F
    
    I[آزور OpenAI (رابط پاسخ‌ها)] --> B
    I --> D
    I --> F
```

**اجزای کلیدی:**
- **عوامل متوالی**: عوامل تخصصی برای هر مرحله پردازش
- **زمینه حالت**: حفظ داده‌ها و تصمیمات انباشته شده در سراسر مراحل
- **نقاط بازرسی**: نقاط اعتبارسنجی بین مراحل برای اطمینان از کیفیت و یکنواختی
- **کلاینت Azure OpenAI**: دسترسی ثابت به مدل AI در تمام مراحل گردش کار

## 🎨 **الگوهای طراحی گردش کار متوالی**

### 📝 **خط لوله پردازش اسناد**
```
Raw Document → Content Extraction → Analysis → Validation → Structured Output
```

### 🎯 **گردش کار تولید محتوا**
```
Brief/Requirements → Research → Content Creation → Review → Final Polish
```

### 🔍 **خط لوله تضمین کیفیت**
```
Initial Review → Technical Validation → Compliance Check → Final Approval
```

### 💼 **گردش کار هوش کسب‌وکار**
```
Data Collection → Processing → Analysis → Report Generation → Distribution
```

## 🏢 **مزایای سازمانی متوالی**

### 🎯 **قابل اعتماد بودن و کیفیت**
- **پردازش تعیین‌پذیر**: نتایج ثابت و تکرارپذیر از طریق مراحل ساختاریافته
- **دروازه‌های کیفیت**: نقاط اعتبارسنجی که کیفیت را در هر مرحله تضمین می‌کنند
- **جداسازی خطا**: مشکلات در یک مرحله به مراحل بعدی سرایت نمی‌کند
- **ردیابی ممیزی**: پیگیری کامل تصمیمات و تبدیل‌ها در هر مرحله

### 📈 **مقیاس‌پذیری و عملکرد**
- **طراحی مدولار**: هر مرحله می‌تواند به طور مستقل بهینه‌سازی شود
- **مدیریت منابع**: تخصیص کارآمد منابع مدل AI در مراحل مختلف
- **بهینه‌سازی حالت**: حداقل انتقال حالت بین مراحل برای عملکرد بهینه
- **گروه‌های مرحله‌ای موازی**: چندین جریان متوالی می‌توانند به صورت موازی اجرا شوند

### 🔒 **امنیت و انطباق**
- **امنیت در سطح مرحله**: سیاست‌های امنیتی متفاوت برای مراحل مختلف پردازش
- **اعتبارسنجی داده‌ها**: اطمینان از یکپارچگی داده و تطابق در هر نقطه بازرسی
- **کنترل دسترسی**: مجوزهای دقیق برای مراحل مختلف گردش کار
- **انطباق مقرراتی**: رعایت الزامات قانونی از طریق پردازش ساختاریافته

### 📊 **نظارت و تحلیل‌ها**
- **شاخص‌های سطح مرحله**: نظارت عملکرد برای هر مرحله گردش کار
- **شناسایی گلوگاه‌ها**: شناسایی و بهینه‌سازی مراحل کند
- **معیارهای کیفیت**: ردیابی کیفیت و نرخ موفقیت در هر مرحله
- **بهینه‌سازی فرایند**: بهبود مستمر مبتنی بر تحلیل‌های سطح مرحله

بیایید خطوط لوله پردازش AI متوالی و مقاوم بسازیم! 🚀

## 💻 اجرای کد

پیاده‌سازی کامل در `02.dotnet-agent-framework-workflow-ghmodel-sequential.cs` موجود است. این فایل یک **گردش کار تحلیل مبلمان سه مرحله‌ای** را نشان می‌دهد:

1. **مرحله ۱ - عامل فروش**: تحلیل تصاویر مبلمان و ارائه پیشنهادات خرید
2. **مرحله ۲ - عامل قیمت**: ارائه تجزیه و تحلیل قیمت دقیق و گزینه‌های بودجه
3. **مرحله ۳ - عامل ارزیابی**: تولید یک سند پی‌نویس حرفه‌ای به فرمت Markdown

### 🏗️ **معماری گردش کار**

```
Image Input → Sales Analysis → Price Estimation → Quote Generation → Final Output
```

هر عامل:
- خروجی مرحله قبلی را به عنوان زمینه دریافت می‌کند
- بر اساس تحلیل قبلی با تخصص خاص توسعه می‌دهد
- پیوستگی گردش کار را از طریق مدیریت حالت حفظ می‌کند

### 🚀 اجرای نمونه

**پیش‌نیازها:**
- قرار دادن تصویر مبلمان در `../imgs/home.png` (یا بروزرسانی متغیر `imgPath`)
- پیکربندی فایل `.env` با نقطه انتهایی و استقرار Azure OpenAI خود، سپس ورود با `az login`

```bash
# اسکریپت را اجرایی کنید (یونیکس/لینوکس/مک‌او‌اس)
chmod +x 02.dotnet-agent-framework-workflow-ghmodel-sequential.cs

# اجرای جریان کاری متوالی
./02.dotnet-agent-framework-workflow-ghmodel-sequential.cs
```

یا در ویندوز:
```powershell
dotnet run 02.dotnet-agent-framework-workflow-ghmodel-sequential.cs
```

### 📝 خروجی مورد انتظار

گردش کار به این صورت عمل می‌کند:
1. **عامل فروش**: شناسایی اقلام مبلمان از تصویر و ارائه توصیه‌ها
2. **عامل قیمت**: افزودن تحلیل قیمت دقیق با رده‌های بودجه و توصیه‌های خرید
3. **عامل ارزیابی**: تولید سند ارزیابی قالب‌بندی شده با همه اطلاعات ترکیب‌شده

خروجی نهایی یک ارزیابی مبلمان جامع و حرفه‌ای بر اساس تحلیل تصویر خواهد بود.

### 🔧 گزینه‌های سفارشی‌سازی

**تغییر رفتار عامل:**
```csharp
// Adjust agent instructions to change their focus
const string SalesAgentInstructions = "Your custom instructions...";
```

**تغییر جریان متوالی:**
```csharp
// Add or reorder workflow stages
var workflow = new WorkflowBuilder(salesagent)
    .AddEdge(salesagent, priceagent)
    .AddEdge(priceagent, quoteagent)
    .AddEdge(quoteagent, newAgent)  // Add another stage
    .Build();
```

**استفاده از ورودی متفاوت:**
```csharp
// Process text instead of images
ChatMessage userMessage = new ChatMessage(ChatRole.User, [
    new TextContent("Analyze pricing for a modern living room set")
]);
```

### 🎯 کاربردهای دنیای واقعی

این الگوی متوالی برای موارد زیر ایده‌آل است:
- **تجارت الکترونیک**: تحلیل محصول → قیمت گذاری → ایجاد ارزیابی
- **املاک و مستغلات**: تحلیل ملک → ارزیابی → ایجاد فهرست
- **بیمه**: تحلیل ادعا → ارزیابی → ایجاد ارزیابی
- **تولید محتوا**: تحقیق → نوشتار → ویرایش → انتشار

### 🔍 درک جریان حالت

هر عامل در دنباله دریافت می‌کند:
- **ورودی اصلی**: پیام اولیه کاربر (تصویر + متن)
- **خروجی‌های عوامل قبلی**: تمام پاسخ‌های قبلی عوامل در تاریخچه گفتگو
- **زمینه انباشته شده**: حالت کامل حفظ شده در طول گردش کار

این امکان را فراهم می‌کند که پردازش چند مرحله‌ای پیچیده انجام شود که هر عامل بر اساس زمینه‌ای جامع از تمام مراحل قبلی بنا می‌کند.

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**سلب مسئولیت**:
این سند با استفاده از سرویس ترجمه هوش مصنوعی [Co-op Translator](https://github.com/Azure/co-op-translator) ترجمه شده است. در حالی که ما در تلاش برای دقت هستیم، لطفاً توجه داشته باشید که ترجمه‌های خودکار ممکن است شامل خطاها یا نادرستی‌هایی باشند. سند اصلی به زبان مادری خود باید به عنوان منبع معتبر در نظر گرفته شود. برای اطلاعات حیاتی، ترجمه حرفه‌ای انسانی توصیه می‌شود. ما در قبال هرگونه سوء تفاهم یا برداشت نادرست ناشی از استفاده از این ترجمه مسئولیتی نداریم.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->