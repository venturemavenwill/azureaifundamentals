# ⚡ Fluxos de Trabalho Concorrentes de Agentes com Azure OpenAI (API de Respostas) (.NET)

## 📋 Tutorial de Processamento Paralelo de Alto Desempenho

Este notebook demonstra **padrões de fluxo de trabalho concorrente** utilizando o Microsoft Agent Framework para .NET e Azure OpenAI (API de Respostas). Vai aprender a construir fluxos de trabalho paralelos de alto desempenho que maximizam a produtividade ao executar múltiplos agentes de IA simultaneamente, mantendo a coordenação e a consistência dos dados.

## 🎯 Objetivos de Aprendizagem

### 🚀 **Fundamentos do Processamento Concorrente**
- **Execução Paralela de Agentes**: Execute múltiplos agentes de IA simultaneamente para máximo desempenho
- **Padrões Async/Await**: Aproveite o modelo de programação assíncrona do .NET para concorrência eficiente
- **Azure OpenAI (API de Respostas)**: Coordene múltiplas chamadas concorrentes à API de Respostas Azure OpenAI
- **Gestão de Recursos**: Gerencie eficientemente recursos de modelos de IA entre operações concorrentes

### 🏗️ **Arquitetura Avançada de Concorrência**
- **Paralelismo Baseado em Tarefas**: Use a Biblioteca Paralela de Tarefas do .NET para execução concorrente ótima
- **Padrões de Sincronização**: Coordene agentes concorrentes evitando condições de corrida
- **Balanceamento de Carga**: Distribua trabalho eficientemente através da capacidade de processamento concorrente disponível
- **Tolerância a Falhas**: Trate falhas individuais de agentes sem parar o fluxo de trabalho completo

### 🏢 **Aplicações Empresariais Concorrentes**
- **Processamento de Documentos em Alto Volume**: Processe múltiplos documentos simultaneamente
- **Análise de Conteúdo em Tempo Real**: Análise concorrente de fluxos de dados recebidos
- **Otimização de Processamento em Lote**: Maximize o rendimento para operações de processamento de dados em larga escala
- **Análise Multi-Modal**: Processamento paralelo de diferentes tipos e formatos de conteúdo

## ⚙️ Pré-requisitos & Configuração

### 📦 **Pacotes NuGet Necessários**

Pacotes essenciais para fluxos de trabalho concorrentes de alto desempenho:

```xml
<!-- Core AI Framework with Async Support -->
<PackageReference Include="Microsoft.Extensions.AI" Version="10.*" />

<!-- Azure OpenAI (Responses API) -->
<PackageReference Include="Azure.AI.OpenAI" Version="2.*" />

<!-- Azure Identity and Async LINQ for Advanced Operations -->
<PackageReference Include="Azure.Identity" Version="1.15.0" />
<PackageReference Include="System.Linq.Async" Version="6.0.3" />

<!-- Local Agent Framework References -->
<!-- Microsoft.Agents.AI.dll - Core agent abstractions with async support -->
<!-- Microsoft.Agents.AI.OpenAI.dll - Azure OpenAI (Responses API) integration with concurrency -->
```

### 🔑 **Configuração Azure OpenAI**

**Configuração do Ambiente (ficheiro .env):**
```env
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-5-mini
```

**Considerações de Processamento Concorrente:**
```csharp
// Configure for concurrent operations
var clientOptions = new AzureOpenAIClientOptions()
{
    // Configure network timeout for concurrent requests
    NetworkTimeout = TimeSpan.FromMinutes(5)
};
```

### 🏗️ **Arquitetura do Fluxo de Trabalho Concorrente**

```mermaid
graph TD
    A[Entrada do Fluxo de Trabalho] --> B[Distribuição de Tarefas]
    B --> C[Pool de Agentes Concorrentes]
    C --> D[Tarefa do Agente 1]
    C --> E[Tarefa do Agente 2]
    C --> F[Tarefa do Agente 3]
    C --> G[Tarefa do Agente N]
    
    D --> H[Agregação de Resultados]
    E --> H
    F --> H
    G --> H
    
    H --> I[Saída Sincronizada]
    
    J[Azure OpenAI (API de Respostas)] --> D
    J --> E
    J --> F
    J --> G
    
    K[Agendador de Tarefas .NET] --> C
```

**Componentes Principais:**
- **Task Parallel Library**: Suporte nativo do .NET para operações concorrentes
- **Pool de Agentes**: Múltiplas instâncias de agente para processamento em paralelo
- **Agregação de Resultados**: Coordenação e fusão dos resultados concorrentes de agentes
- **Pontos de Sincronização**: Garantir consistência de dados em operações concorrentes

## 🎨 **Padrões de Design para Fluxos de Trabalho Concorrentes**

### 🔍 **Investigação & Análise Paralelas**
```
Research Topic → Concurrent Research Agents → Result Synthesis → Final Report
```

### 📊 **Processamento de Dados Multi-Fonte**
```
Data Sources → Parallel Processing Agents → Data Integration → Unified Output
```

### 🎭 **Pipeline de Geração de Conteúdo**
```
Content Requirements → Concurrent Content Generators → Quality Review → Final Content
```

### 🔄 **Processamento Fan-Out/Fan-In**
```
Single Input → Multiple Concurrent Processors → Result Aggregation → Single Output
```

## 🏢 **Benefícios de Desempenho Empresariais**

### ⚡ **Rendimento & Escalabilidade**
- **Escalonamento de Desempenho Linear**: Adicione mais agentes concorrentes para aumentar o rendimento
- **Utilização de Recursos**: Eficiência máxima da capacidade disponível dos modelos de IA
- **Redução do Tempo de Processamento**: Redução significativa do tempo através da execução paralela
- **Escalonamento Elástico**: Ajuste dinâmico do número de agentes concorrentes com base na carga de trabalho

### 🛡️ **Confiabilidade & Resiliência**
- **Isolamento de Falhas**: Falhas individuais de agentes não afetam outras operações concorrentes
- **Degradação Suave**: O sistema continua a operar com capacidade reduzida de agentes
- **Recuperação de Erros**: Mecanismos automáticos de repetição para operações concorrentes falhadas
- **Distribuição de Carga**: Distribuição uniforme do trabalho entre agentes disponíveis

### 📊 **Monitorização de Desempenho**
- **Métricas de Execução Concorrente**: Acompanhe o desempenho de todas as operações paralelas
- **Análises de Utilização de Recursos**: Monitorize CPU, memória e utilização de rede
- **Análise de Rendimento**: Meça os ganhos de eficiência do processamento concorrente
- **Detecção de Gargalos**: Identifique e resolva restrições de desempenho

### 🔧 **Desenvolvimento & Operações**
- **Modelo de Programação Assíncrono**: Aproveite os padrões maduros async/await do .NET
- **Coordenação de Tarefas**: Capacidades embutidas de gestão e coordenação de tarefas
- **Tratamento de Exceções**: Gestão abrangente de erros para operações concorrentes
- **Suporte ao Debugging**: Ferramentas de depuração do Visual Studio para fluxos de trabalho concorrentes

Vamos construir fluxos de trabalho concorrentes de IA de alto desempenho com .NET! 🚀

## 💻 Executar o Código

A implementação completa está disponível em `03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs`. Este ficheiro demonstra um **fluxo de trabalho concorrente Fan-Out/Fan-In** para planeamento de viagens:

### 🏗️ **Arquitetura do Fluxo de Trabalho**

```
User Request → ConcurrentStartExecutor → [Researcher Agent || Planner Agent] → ConcurrentAggregationExecutor → Final Output
```

**Componentes Principais:**

1. **ConcurrentStartExecutor**: Transmite o pedido do utilizador a todos os agentes simultaneamente
2. **Agente Investigador**: Analisa destinos e atrações em paralelo
3. **Agente Planeador**: Cria planos de viagem detalhados em paralelo
4. **ConcurrentAggregationExecutor**: Recolhe e funde resultados de ambos os agentes

### 🎯 **Padrão Fan-Out/Fan-In**

Este fluxo de trabalho demonstra o clássico padrão **Fan-Out/Fan-In**:
- **Fan-Out**: Uma mensagem de entrada é transmitida para múltiplos agentes simultaneamente
- **Processamento Concorrente**: Vários agentes trabalham em paralelo na mesma tarefa
- **Fan-In**: Os resultados de todos os agentes são recolhidos e agregados numa única saída

### 🚀 Executar o Exemplo

```bash
# Torne o script executável (Unix/Linux/macOS)
chmod +x 03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs

# Execute o fluxo de trabalho concorrente
./03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs
```

Ou no Windows:
```powershell
dotnet run 03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs
```

### 📝 Saída Esperada

O fluxo de trabalho irá:
1. **Transmitir Pedido**: Enviar "Planear uma viagem para Seattle em Dezembro" para ambos os agentes
2. **Processamento Concorrente**: Ambos os agentes trabalham simultaneamente:
   - Investigador identifica atrações e detalhes
   - Planeador cria itinerário e logística
3. **Agregação**: Combinar ambas as respostas numa saída abrangente
4. **Exibir Resultados**: Mostrar o plano de viagem combinado com toda a informação

### 🔧 Opções de Personalização

**Adicionar Mais Agentes Concorrentes:**
```csharp
// Create additional specialized agents
AIAgent budgetAgent = azureClient.GetChatClient(deployment).AsAIAgent(
    name: "Budget-Agent", instructions: "Calculate travel costs...");

// Add to fan-out
var workflow = new WorkflowBuilder(startExecutor)
    .AddFanOutEdge(startExecutor, targets: [researcherAgent, plannerAgent, budgetAgent])
    .AddFanInBarrierEdge(sources: [researcherAgent, plannerAgent, budgetAgent], aggregationExecutor)
    .WithOutputFrom(aggregationExecutor)
    .Build();

// Update aggregation count
if (this._messages.Count == 3) { ... }
```

**Modificar Instruções dos Agentes:**
```csharp
const string ResearcherAgentInstructions = "Your custom instructions for research...";
const string PlanAgentInstructions = "Your custom instructions for planning...";
```

**Alterar a Tarefa:**
```csharp
StreamingRun run = await InProcessExecution.RunStreamingAsync(
    workflow, 
    "Plan a European vacation for 2 weeks in summer"
);
```

### 🎯 Aplicações no Mundo Real

Este padrão concorrente é ideal para:
- **Criação de Conteúdo**: Vários escritores criando diferentes secções simultaneamente
- **Revisão de Código**: Vários revisores a analisar código de diferentes perspetivas
- **Pesquisa de Mercado**: Análise paralela de diferentes segmentos de mercado
- **Processamento de Documentos**: Extração, análise e validação concorrentes
- **Análise Multi-Perspetiva**: Obtenção de diversos pontos de vista sobre a mesma entrada

### 🔍 Compreender Executors Personalizados

**ConcurrentStartExecutor:**
- Implementa `IMessageHandler<string>` para aceitar entradas de texto
- Transmite mensagens para todos os agentes ligados
- Envia `TurnToken` para desencadear processamento concorrente

**ConcurrentAggregationExecutor:**
- Implementa `IMessageHandler<ChatMessage>` para receber respostas dos agentes
- Recolhe mensagens de forma thread-safe
- Agrega quando todas as respostas esperadas chegam
- Produz a saída final usando `context.YieldOutputAsync()`

### ⚡ Benefícios de Desempenho

**Concorrente vs Sequencial:**
- Sequencial: Agente1 (30s) → Agente2 (30s) = **60 segundos no total**
- Concorrente: Agente1 (30s) || Agente2 (30s) = **30 segundos no total**

**Melhoria do rendimento**: Até N× mais rápido para N agentes concorrentes (dependendo da carga e recursos)

### 🛡️ Tratamento de Erros

O fluxo de trabalho trata falhas de agentes individuais com suavidade:
- Se um agente falhar, os outros continuam a processar
- O agregador pode implementar lógica de timeout
- Resultados parciais podem ser retornados se necessário

### 📊 Funcionalidades Avançadas

**Número Dinâmico de Agentes:**
Modifique a lógica de agregação para suportar contagens variáveis de agentes:

```csharp
private int _expectedAgentCount;
private readonly List<ChatMessage> _messages = [];

public override async ValueTask HandleAsync(List<ChatMessage> message, IWorkflowContext context, CancellationToken cancellationToken = default)
{
    this._messages.AddRange(message);
    if (this._messages.Count == _expectedAgentCount)
    {
        // Process aggregation
    }
}
```

Este padrão de fluxo de trabalho concorrente é essencial para construir sistemas de agentes de IA escaláveis e de alto desempenho!

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Aviso Legal**:
Este documento foi traduzido utilizando o serviço de tradução automática [Co-op Translator](https://github.com/Azure/co-op-translator). Embora nos esforcemos pela precisão, esteja ciente de que traduções automáticas podem conter erros ou imprecisões. O documento original na sua língua nativa deve ser considerado a fonte autorizada. Para informações críticas, recomenda-se tradução profissional humana. Não nos responsabilizamos por quaisquer mal-entendidos ou interpretações incorretas resultantes da utilização desta tradução.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->