[project]
name = "zava-shop-shared"
version = "0.1.0"
description = "Shared utilities and configurations for the shop application"

requires-python = ">=3.12"
dependencies = [
    "pydantic",
    "aiosqlite>=0.20.0,<0.21.0",
    "sqlalchemy[asyncio]>=2.0.0,<3.0.0",
]