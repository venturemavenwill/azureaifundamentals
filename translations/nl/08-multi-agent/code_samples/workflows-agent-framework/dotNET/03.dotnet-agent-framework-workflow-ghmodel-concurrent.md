# ⚡ Gelijktijdige Agent Workflows met Azure OpenAI (Responses API) (.NET)

## 📋 High-Performance Parallel Verwerking Tutor

Dit notebook demonstreert **concurrente workflowpatronen** met behulp van het Microsoft Agent Framework voor .NET en Azure OpenAI (Responses API). Je leert hoe je high-performance, parallelle verwerkingsworkflows bouwt die de doorvoer maximaliseren door meerdere AI-agents gelijktijdig uit te voeren, terwijl coördinatie en gegevensconsistentie behouden blijven.

## 🎯 Leerdoelen

### 🚀 **Grondbeginselen Gelijktijdige Verwerking**
- **Parallel Agent Uitvoering**: Voer meerdere AI-agents gelijktijdig uit voor maximale prestaties
- **Async/Await Patronen**: Gebruik het async programmeermodel van .NET voor efficiënte gelijktijdigheid
- **Azure OpenAI (Responses API)**: Coördineer meerdere gelijktijdige aanroepen naar de Azure OpenAI Responses API
- **Resourcebeheer**: Beheer AI-modelresources efficiënt over gelijktijdige operaties

### 🏗️ **Geavanceerde Gelijktijdigheidsarchitectuur**
- **Taakgebaseerde Parallelisme**: Gebruik .NET Task Parallel Library voor optimale gelijktijdige uitvoering
- **Synchronisatiepatronen**: Coördineer gelijktijdige agents en voorkom racecondities
- **Load Balancing**: Verdeel werkzaamheden efficiënt over beschikbare gelijktijdige verwerkingscapaciteit
- **Faalbestendigheid**: Handel individuele agentfouten af zonder de hele workflow te stoppen

### 🏢 **Enterprise Gelijktijdige Applicaties**
- **High-Volume Documentverwerking**: Verwerk meerdere documenten gelijktijdig
- **Realtime Contentanalyse**: Gelijktijdige analyse van binnenkomende datastromen
- **Batchverwerking Optimalisatie**: Maximaliseer doorvoer voor grootschalige dataverwerkingsoperaties
- **Multi-Modale Analyse**: Parallelle verwerking van verschillende contenttypen en formaten

## ⚙️ Vereisten & Setup

### 📦 **Vereiste NuGet Packages**

Essentiële pakketten voor high-performance gelijktijdige workflows:

```xml
<!-- Core AI Framework with Async Support -->
<PackageReference Include="Microsoft.Extensions.AI" Version="10.*" />

<!-- Azure OpenAI (Responses API) -->
<PackageReference Include="Azure.AI.OpenAI" Version="2.*" />

<!-- Azure Identity and Async LINQ for Advanced Operations -->
<PackageReference Include="Azure.Identity" Version="1.15.0" />
<PackageReference Include="System.Linq.Async" Version="6.0.3" />

<!-- Local Agent Framework References -->
<!-- Microsoft.Agents.AI.dll - Core agent abstractions with async support -->
<!-- Microsoft.Agents.AI.OpenAI.dll - Azure OpenAI (Responses API) integration with concurrency -->
```

### 🔑 **Azure OpenAI Configuratie**

**Omgevingsinstellingen (.env bestand):**
```env
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-5-mini
```

**Overwegingen bij gelijktijdige verwerking:**
```csharp
// Configure for concurrent operations
var clientOptions = new AzureOpenAIClientOptions()
{
    // Configure network timeout for concurrent requests
    NetworkTimeout = TimeSpan.FromMinutes(5)
};
```

### 🏗️ **Concurrent Workflow Architectuur**

```mermaid
graph TD
    A[Workflow Invoer] --> B[Taakverdeling]
    B --> C[Gelijktijdige Agentenpool]
    C --> D[Agentaak 1]
    C --> E[Agentaak 2]
    C --> F[Agentaak 3]
    C --> G[Agentaak N]
    
    D --> H[Resultaataggregatie]
    E --> H
    F --> H
    G --> H
    
    H --> I[Gesynchroniseerde Uitvoer]
    
    J[Azure OpenAI (Responses API)] --> D
    J --> E
    J --> F
    J --> G
    
    K[.NET Taakplanner] --> C
```

**Belangrijke componenten:**
- **Task Parallel Library**: Ingebouwde .NET ondersteuning voor gelijktijdige operaties
- **Agent Pool**: Meerdere agentinstanties voor parallelle verwerking
- **Resultaataggregatie**: Coördinatie en samenvoeging van gelijktijdige agentresultaten
- **Synchronisatiepunten**: Zorg voor gegevensconsistentie over gelijktijdige operaties

## 🎨 **Concurrent Workflow Ontwerp Patronen**

### 🔍 **Parallel Onderzoek & Analyse**
```
Research Topic → Concurrent Research Agents → Result Synthesis → Final Report
```

### 📊 **Multi-Bronnen Data Verwerking**
```
Data Sources → Parallel Processing Agents → Data Integration → Unified Output
```

### 🎭 **Content Generatie Pipeline**
```
Content Requirements → Concurrent Content Generators → Quality Review → Final Content
```

### 🔄 **Fan-Out/Fan-In Verwerking**
```
Single Input → Multiple Concurrent Processors → Result Aggregation → Single Output
```

## 🏢 **Enterprise Prestatievoordelen**

### ⚡ **Doorvoer & Schaalbaarheid**
- **Lineaire Prestatie Schaling**: Voeg meer gelijktijdige agents toe om de doorvoer te verhogen
- **Resourcegebruik**: Maximale efficiëntie van beschikbare AI-modelcapaciteit
- **Verminderde Verwerkingstijd**: Significante tijdsbesparing door parallelle uitvoering
- **Elastische Schaling**: Dynamisch aanpassen van het aantal gelijktijdige agents op basis van de werklast

### 🛡️ **Betrouwbaarheid & Veerkracht**
- **Foutisolatie**: Individuele agentfouten hebben geen invloed op andere gelijktijdige operaties
- **Graceful Degradation**: Systeem blijft werken met verminderde agentcapaciteit
- **Foutherstel**: Automatische herhaalmechanismen voor mislukte gelijktijdige operaties
- **Belastingsverdeling**: Gelijke verdeling van werk over beschikbare agents

### 📊 **Prestatiemonitoring**
- **Metrics Gelijktijdige Uitvoering**: Volg prestaties van alle parallelle operaties
- **Resourcegebruik Analyse**: Monitor CPU-, geheugen- en netwerkgebruik
- **Doorvoeranalyse**: Meet efficiëntiewinst van gelijktijdige verwerking
- **Bottleneck Detectie**: Identificeer en los prestatieknelpunten op

### 🔧 **Ontwikkeling & Operaties**
- **Async Programmeermodel**: Gebruik de volwassen async/await-patronen van .NET
- **Taakcoördinatie**: Ingebouwde taakbeheer- en coördinatiefunctionaliteiten
- **Foutafhandeling**: Uitgebreide foutafhandeling voor gelijktijdige operaties
- **Debugging Ondersteuning**: Visual Studio debugging tools voor gelijktijdige workflows

Laten we high-performance gelijktijdige AI-workflows bouwen met .NET! 🚀

## 💻 Code Uitvoeren

De volledige implementatie is beschikbaar in `03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs`. Dit bestand demonstreert een **Fan-Out/Fan-In gelijktijdige workflow** voor reisplanning:

### 🏗️ **Workflow Architectuur**

```
User Request → ConcurrentStartExecutor → [Researcher Agent || Planner Agent] → ConcurrentAggregationExecutor → Final Output
```

**Belangrijke componenten:**

1. **ConcurrentStartExecutor**: Zendt het gebruikersverzoek gelijktijdig naar alle agents
2. **Researcher Agent**: Analyseert bestemmingen en attracties gelijktijdig
3. **Planner Agent**: Maakt gedetailleerde reisplannen gelijktijdig
4. **ConcurrentAggregationExecutor**: Verzamelt en voegt resultaten van beide agents samen

### 🎯 **Fan-Out/Fan-In Patron**

Deze workflow demonstreert het klassieke **Fan-Out/Fan-In** patroon:
- **Fan-Out**: Eén invoerbericht wordt gelijktijdig uitgezonden naar meerdere agents
- **Gelijktijdige Verwerking**: Meerdere agents werken parallel aan dezelfde taak
- **Fan-In**: Resultaten van alle agents worden verzameld en samen gevoegd tot één output

### 🚀 Voorbeeld Uitvoeren

```bash
# Maak het script uitvoerbaar (Unix/Linux/macOS)
chmod +x 03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs

# Voer de gelijktijdige workflow uit
./03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs
```

Of op Windows:
```powershell
dotnet run 03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs
```

### 📝 Verwachte Output

De workflow zal:
1. **Verzoek Uitzenden**: Zend "Plan een trip naar Seattle in december" naar beide agents
2. **Gelijktijdige Verwerking**: Beide agents werken gelijktijdig:
   - Onderzoeker identificeert attracties en details
   - Planner maakt reisroute en logistiek
3. **Aggregatie**: Combineer beide antwoorden tot één uitgebreid resultaat
4. **Resultaten Tonen**: Toon het samengevoegde reisplan met alle informatie

### 🔧 Aanpassingsopties

**Voeg Meer Gelijktijdige Agents Toe:**
```csharp
// Create additional specialized agents
AIAgent budgetAgent = azureClient.GetChatClient(deployment).AsAIAgent(
    name: "Budget-Agent", instructions: "Calculate travel costs...");

// Add to fan-out
var workflow = new WorkflowBuilder(startExecutor)
    .AddFanOutEdge(startExecutor, targets: [researcherAgent, plannerAgent, budgetAgent])
    .AddFanInBarrierEdge(sources: [researcherAgent, plannerAgent, budgetAgent], target: aggregationExecutor)
    .WithOutputFrom(aggregationExecutor)
    .Build();

// Update aggregation count
if (this._messages.Count == 3) { ... }
```

**Wijzig Agentinstructies:**
```csharp
const string ResearcherAgentInstructions = "Your custom instructions for research...";
const string PlanAgentInstructions = "Your custom instructions for planning...";
```

**Verander de Taak:**
```csharp
StreamingRun run = await InProcessExecution.RunStreamingAsync(
    workflow, 
    "Plan a European vacation for 2 weeks in summer"
);
```

### 🎯 Toepassingen in de Praktijk

Dit gelijktijdige patroon is ideaal voor:
- **Contentcreatie**: Meerdere schrijvers maken gelijktijdig verschillende secties
- **Code Review**: Meerdere beoordelaars analyseren code vanuit verschillende perspectieven
- **Marktonderzoek**: Parallelle analyse van verschillende marktsegmenten
- **Documentverwerking**: Gelijktijdige extractie, analyse en validatie
- **Multi-Perspectief Analyse**: Verschillende gezichtspunten op dezelfde input verkrijgen

### 🔍 Begrip van Custom Executors

**ConcurrentStartExecutor:**
- Implementeert `IMessageHandler<string>` voor het accepteren van string input
- Zendt berichten naar alle verbonden agents
- Stuurt `TurnToken` om gelijktijdige verwerking te activeren

**ConcurrentAggregationExecutor:**
- Implementeert `IMessageHandler<ChatMessage>` om agentantwoorden te ontvangen
- Verzamelt berichten op een thread-safe manier
- Voert aggregatie uit als alle verwachte antwoorden zijn binnengekomen
- Geeft de finale output via `context.YieldOutputAsync()`

### ⚡ Prestatievoordelen

**Gelijktijdig vs Sequentieel:**
- Sequentieel: Agent1 (30s) → Agent2 (30s) = **60 seconden totaal**
- Gelijktijdig: Agent1 (30s) || Agent2 (30s) = **30 seconden totaal**

**Doorvoerverbetering**: Tot N× sneller voor N gelijktijdige agents (afhankelijk van werklast en resources)

### 🛡️ Foutafhandeling

De workflow handelt individuele agentstoringen soepel af:
- Als een agent faalt, blijven andere agents verwerken
- Aggregator kan timeout-logica implementeren
- Indien nodig kunnen gedeeltelijke resultaten worden teruggegeven

### 📊 Geavanceerde Functionaliteiten

**Dynamisch Aantal Agents:**
Pas de aggregatielogica aan om variabele aantallen agents te ondersteunen:

```csharp
private int _expectedAgentCount;
private readonly List<ChatMessage> _messages = [];

public override ValueTask HandleAsync(ChatMessage message, IWorkflowContext context, CancellationToken cancellationToken = default)
{
    this._messages.Add(message);
    if (this._messages.Count == _expectedAgentCount)
    {
        // Process aggregation
    }
    
    return ValueTask.CompletedTask;
}
```

Dit gelijktijdige workflowpatroon is essentieel voor het bouwen van high-performance, schaalbare AI-agent systemen!

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Disclaimer**:
Dit document is vertaald met behulp van de AI vertaaldienst [Co-op Translator](https://github.com/Azure/co-op-translator). Hoewel we streven naar nauwkeurigheid, dient u er rekening mee te houden dat geautomatiseerde vertalingen fouten of onnauwkeurigheden kunnen bevatten. Het originele document in de oorspronkelijke taal moet worden beschouwd als de gezaghebbende bron. Voor kritieke informatie wordt professionele menselijke vertaling aanbevolen. Wij zijn niet aansprakelijk voor eventuele misverstanden of verkeerde interpretaties die voortvloeien uit het gebruik van deze vertaling.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->