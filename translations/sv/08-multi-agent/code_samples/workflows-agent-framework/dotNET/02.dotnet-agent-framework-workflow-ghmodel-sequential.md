# ⏩ Sekventiella agentarbetsflöden med Azure OpenAI (Responses API) (.NET)

## 📋 Avancerad handledning för sekventiell bearbetning

Denna anteckningsbok demonstrerar **sekventiella arbetsflödesmönster** med Microsoft Agent Framework för .NET och Azure OpenAI (Responses API). Du lär dig hur man bygger sofistikerade, steg-för-steg bearbetningspipelines där agenter körs i en specifik ordning, där varje steg bygger på resultatet från föregående steg.

## 🎯 Lärandemål

### 🔄 **Sekventiell bearbetningsarkitektur**
- **Linjär arbetsflödesdesign**: Skapa steg-för-steg bearbetningspipelines med tydliga beroenden
- **Tillståndshantering**: Behåll kontext och dataflöde över sekventiella arbetsflödestege
- **Azure OpenAI (Responses API)**: Använd Azure OpenAI-modeller i fler-stegs .NET-arbetsflöden
- **Företagspipeline-mönster**: Bygg produktionsklara sekventiella bearbetningssystem

### 🏗️ **Avancerade sekventiella mönster**
- **Stage-Gate-bearbetning**: Implementera valideringskontroller mellan arbetsflödestege
- **Kontextbevarande**: Behåll tillstånd och ackumulerad kunskap över alla steg
- **Felpropagering**: Hantera fel smidigt i sekventiella bearbetningskedjor
- **Prestandaoptimering**: Effektiv sekventiell exekvering med minimalt overhead

### 🏢 **Sekventiella företagsapplikationer**
- **Dokumentbearbetningspipeline**: Fler-stegs dokumentanalys, transformation och validering
- **Kvalitetssäkringsarbetsflöden**: Sekventiell granskning, validering och godkännandeprocesser
- **Innehållsproduktionspipeline**: Forskning → Skrivande → Redigering → Granskning → Publicering
- **Automatisering av affärsprocesser**: Flerstegs affärsarbetflöden med tydliga stegberoenden

## ⚙️ Förutsättningar och installation

### 📦 **Nödvändiga NuGet-paket**

Viktiga paket för sekventiella .NET-arbetsflöden:

```xml
<!-- Core AI Framework -->
<PackageReference Include="Microsoft.Extensions.AI" Version="10.*" />

<!-- Azure OpenAI (Responses API) -->
<PackageReference Include="Azure.AI.OpenAI" Version="2.*" />

<!-- Azure Identity and Async LINQ Support -->
<PackageReference Include="Azure.Identity" Version="1.15.0" />
<PackageReference Include="System.Linq.Async" Version="6.0.3" />

<!-- Local Agent Framework References -->
<!-- Microsoft.Agents.AI.dll - Core agent abstractions -->
<!-- Microsoft.Agents.AI.OpenAI.dll - Azure OpenAI (Responses API) integration -->
```

### 🔑 **Azure OpenAI-konfiguration**

**Miljöinställning (.env-fil):**
```env
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-4.1-mini
```

**Konfigurationshantering:**
```csharp
// Load environment variables securely
Env.Load("../../../.env");
var azureEndpoint = Environment.GetEnvironmentVariable("AZURE_OPENAI_ENDPOINT");
var deployment = Environment.GetEnvironmentVariable("AZURE_OPENAI_DEPLOYMENT");
```

### 🏗️ **Sekventiell arbetsflödesarkitektur**

```mermaid
graph TD
    A[Initial Input] --> B[Stage 1: Analysagent]
    B --> C[Kontrollpunkt 1]
    C --> D[Stage 2: Bearbetningsagent]
    D --> E[Kontrollpunkt 2]
    E --> F[Stage 3: Valideringsagent]
    F --> G[Slutlig utdata]
    
    H[Tillståndskontext] --> B
    H --> D
    H --> F
    
    I[Azure OpenAI (Responses API)] --> B
    I --> D
    I --> F
```

**Nyckelkomponenter:**
- **Sekventiella agenter**: Specialiserade agenter för varje bearbetningssteg
- **Tillståndskontext**: Behåller ackumulerad data och beslut över steg
- **Kontrollpunkter**: Valideringspunkter mellan steg för att säkerställa kvalitet och konsekvens
- **Azure OpenAI-klient**: Konsekvent AI-modelltillgång över alla arbetsflödestege

## 🎨 **Designmönster för sekventiella arbetsflöden**

### 📝 **Pipeline för dokumentbearbetning**
```
Raw Document → Content Extraction → Analysis → Validation → Structured Output
```

### 🎯 **Arbetsflöde för innehållsskapande**
```
Brief/Requirements → Research → Content Creation → Review → Final Polish
```

### 🔍 **Pipeline för kvalitetssäkring**
```
Initial Review → Technical Validation → Compliance Check → Final Approval
```

### 💼 **Arbetsflöde för affärsinsikt**
```
Data Collection → Processing → Analysis → Report Generation → Distribution
```

## 🏢 **Företagsfördelar med sekventiell bearbetning**

### 🎯 **Tillförlitlighet och kvalitet**
- **Deterministisk bearbetning**: Konsekventa, upprepbara resultat genom strukturerade steg
- **Kvalitetsgrindar**: Valideringskontroller säkerställer kvalitet vid varje steg
- **Felisolering**: Problem i ett steg sprider sig inte till efterföljande steg
- **Revisionsspår**: Fullständig spårning av beslut och transformationer vid varje steg

### 📈 **Skalbarhet och prestanda**
- **Modulär design**: Varje steg kan optimeras självständigt
- **Resurshantering**: Effektiv fördelning av AI-modellresurser över steg
- **Tillståndsoptimering**: Minimal överföring av tillstånd mellan steg för optimal prestanda
- **Parallella steggrupper**: Flera sekventiella arbetsflöden kan köras parallellt

### 🔒 **Säkerhet och regelefterlevnad**
- **Säkerhet på steg-nivå**: Olika säkerhetspolicys för olika bearbetningssteg
- **Datavalidering**: Säkerställ dataintegritet och efterlevnad vid varje kontrollpunkt
- **Behörighetskontroll**: Granulär behörighet för olika arbetsflödestege
- **Regelkrav**: Uppfyll regelkrav genom strukturerad bearbetning

### 📊 **Övervakning och analys**
- **Mätningsdata på steg-nivå**: Prestandaövervakning för varje arbetsflödestege
- **Flaskhalsidentifiering**: Identifiera och optimera långsamma steg
- **Kvalitetsmått**: Spåra kvalitet och framgångsfrekvenser vid varje steg
- **Processoptimering**: Kontinuerlig förbättring baserad på steg-nivåanalys

Låt oss bygga robusta sekventiella AI-bearbetningspipelines! 🚀

## 💻 Köra koden

Den fullständiga implementeringen finns i `02.dotnet-agent-framework-workflow-ghmodel-sequential.cs`. Denna fil demonstrerar ett **trestegs arbetsflöde för möbelanalys**:

1. **Steg 1 - Säljagent**: Analyserar möbelbilder och ger inköpsförslag
2. **Steg 2 - Prisagent**: Ger detaljerade prisuppdelningar och budgetalternativ
3. **Steg 3 - Offertagent**: Genererar ett professionellt offertdokument i Markdown-format

### 🏗️ **Arbetsflödesarkitektur**

```
Image Input → Sales Analysis → Price Estimation → Quote Generation → Final Output
```

Varje agent:
- Tar emot utdata från föregående steg som kontext
- Bygger vidare på tidigare analys med specialiserad expertis
- Behåller arbetsflödets kontinuitet genom tillståndshantering

### 🚀 Köra exemplet

**Förutsättningar:**
- Placera en möbelbild på `../imgs/home.png` (eller uppdatera variabeln `imgPath`)
- Konfigurera din `.env`-fil med din Azure OpenAI-endpoint och distribution, logga sedan in med `az login`

```bash
# Gör skriptet körbart (Unix/Linux/macOS)
chmod +x 02.dotnet-agent-framework-workflow-ghmodel-sequential.cs

# Kör det sekventiella arbetsflödet
./02.dotnet-agent-framework-workflow-ghmodel-sequential.cs
```

Eller på Windows:
```powershell
dotnet run 02.dotnet-agent-framework-workflow-ghmodel-sequential.cs
```

### 📝 Förväntad utdata

Arbetsflödet kommer att:
1. **Säljagent**: Identifiera möbelobjekt från bilden och ge rekommendationer
2. **Prisagent**: Lägga till detaljerad prisanalys med budgetnivåer och inköpsrekommendationer
3. **Offertagent**: Generera ett formaterat offertdokument med all information sammanställd

Slutresultatet blir en omfattande, professionell möbeloffert baserad på bildanalys.

### 🔧 Anpassningsalternativ

**Ändra agentbeteende:**
```csharp
// Adjust agent instructions to change their focus
const string SalesAgentInstructions = "Your custom instructions...";
```

**Ändra sekventiellt flöde:**
```csharp
// Add or reorder workflow stages
var workflow = new WorkflowBuilder(salesagent)
    .AddEdge(salesagent, priceagent)
    .AddEdge(priceagent, quoteagent)
    .AddEdge(quoteagent, newAgent)  // Add another stage
    .Build();
```

**Använd annan indata:**
```csharp
// Process text instead of images
ChatMessage userMessage = new ChatMessage(ChatRole.User, [
    new TextContent("Analyze pricing for a modern living room set")
]);
```

### 🎯 Realtidsapplikationer

Detta sekventiella mönster är idealiskt för:
- **E-handel**: Produktanalys → Prissättning → Offertgenerering
- **Fastigheter**: Fastighetsanalys → Värdering → Skapande av annons
- **Försäkring**: Skadeanalys → Bedömning → Offertgenerering
- **Innehållsskapande**: Forskning → Skrivande → Redigering → Publicering

### 🔍 Förståelse för tillståndsflöde

Varje agent i sekvensen tar emot:
- **Ursprunglig indata**: Det initiala användarmeddelandet (bild + text)
- **Tidigare agenters utdata**: Alla tidigare agenters svar i konversationshistoriken
- **Ackumulerad kontext**: Komplett tillstånd som upprätthålls genom hela arbetsflödet

Detta möjliggör sofistikerad fler-stegs bearbetning där varje agent bygger vidare på omfattande kontext från alla tidigare steg.

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Ansvarsfriskrivning**:
Detta dokument har översatts med hjälp av AI-översättningstjänsten [Co-op Translator](https://github.com/Azure/co-op-translator). Även om vi strävar efter noggrannhet, var vänlig notera att automatiska översättningar kan innehålla fel eller brister. Det ursprungliga dokumentet på dess modersmål bör betraktas som den auktoritativa källan. För kritisk information rekommenderas professionell mänsklig översättning. Vi ansvarar inte för några missförstånd eller feltolkningar som uppstår till följd av användningen av denna översättning.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->